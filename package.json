{
  "name": "electron-quick-start",
  "version": "1.0.0",
  "description": "A minimal Electron application",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build:osx": "electron-packager . --out=distinct --overwrite --platform=darwin --arch=x64",
    "build:win64": "electron-packager . --out=distinct --overwrite --platform=win32 --arch=x64",
    "build:arm": "electron-packager. --out=distinct --overwrite --platform=linux --arch=armv7l",
    "build:linux": "electron-packager . --out=distinct --overwrite --platform=linux --arch=x64",
    "build": "electron-packager. --out=distinct --asar --overwrite --all"
  },
  "repository": "https://github.com/electron/electron-quick-start",
  "keywords": [
    "Electron",
    "quick",
    "start",
    "tutorial",
    "demo"
  ],
  "author": "GitHub",
  "license": "CC0-1.0",
  "devDependencies": {
    "electron": "^13.6.6"
  },
  "dependencies": {
    "devtron": "^1.4.0",
    "electron-packager": "^15.0.0"
  }
}
